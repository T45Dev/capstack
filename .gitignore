node_modules
.output
.nuxt
.nitro
.cache
dist
*.log
.DS_Store
data/*.db
data/*.db-journal
data/*.db-shm
data/*.db-wal
.env
.env.local

# Cloudflare Tunnel credentials — secret, never commit. Ingress (config.yml) is tracked.
cloudflared/credentials.json
cloudflared/*.json
cloudflared/cert.pem

# Raw, real Carta exports — may contain real PII (names, holdings, $).
# Kept local for analysis only; never committed. De-identified fixtures
# (synthetic names/numbers, real structure) go in a tracked dir instead.
fixtures/private/
